# Legacy v1: Raw-Space Diffusion

This folder contains the original version of **SpectralDiffusionLab**, where
diffusion was trained directly in the raw space and a separate
autoencoder was used for reconstruction.

It is kept here for historical and reproducibility purposes.  
The **current pipeline** is the latent-space diffusion model described in the main `README.md` and implemented under `src/`.
